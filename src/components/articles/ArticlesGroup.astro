---
import ArticleCard from "./ArticleCard.astro";
const {posts} = Astro.props;
---


<div class="px-6 grid grid-cols-1 gap-x-16 gap-y-12 md:grid-cols-2 lg:grid-cols-3 xl:gap-y-16 xl:gap-x-24">
    {
        posts.map((p) => (<ArticleCard post={p}/>))
    }
</div>